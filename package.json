{
  "name": "crossing-over-with-dez",
  "version": "1.0.0",
  "description": "Spiritual Mediumship and Grief Support Platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "esbuild src/index.tsx --bundle --outfile=dist/js/app.js --format=esm --external:react --external:react-dom"
  },
  "dependencies": {
    "@google/genai": "^1.31.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}